import {layouts} from './keyboard-layouts';

export const config = {
    login: '',
    password: '',
    token: '',
    layouts: [layouts.EN, layouts.RU],
    mainEnLayout: layouts.EN,
};

type Config = typeof config;

export default config;
